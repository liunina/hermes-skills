# Upstream Notes

Source repository: `https://github.com/liangdabiao/ecom-details-image`

Imported scope:

- `scripts/generate_image.py`
- `references/templates/*.json`
- safe `.env.example`

Excluded scope:

- generated images
- sample product photos
- screenshots
- root `.env.example` from upstream, because it contained a commented example value that resembled a real API key

The upstream repository did not declare a license when this skill was added. Review authorization before redistributing, publishing releases, or merging into a production skill tap.
